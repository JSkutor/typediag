target text progression branch 작업:

이후


입력, 통계 이 두개가 메인 기능임.
근데 저 브랜치 작업 끝나면 입력 부분은 전반적인 디자인 말고는 더 만질 부분이 없음.

- surface 차이 더 극적으로 보이게 조작.
- 벡터 쪽도 너무 작음.
- 3d 두개는 됐고 text적인 통계는 어떻게 어디에 띄울건지.
- , . 뻬



## 서브 통계
모두 기본 three 3d keyboard surface layout을 바탕으로 함.
surface는 달라짐. 직선, 색 등의 요소를 추가할 예정.

### 스페이스
- from key -> space 의 각 키별 latency
- 다른 키를 눌러야 하는데 space를 눌러서 틀린 경우. 어떤 키를 스페이스보다 빨리 누르는지

### 백스페이스
- 백스페이스 이전 키; 무슨  키를 많이 틀리는지.
- 연속 백스페이스 카운트
- 맞고 틀림을 같이 분석해서
- 틀림 반응속도

### 시프트
- 누르고 있는 지속시간
- 시프트를 생략하고 원래 연속적인 정답 상의 키 순서와 비교해서 시프트 때문에 얼마나 느려졌는지
- 시프트를 눌러야 하는데 키보다 늦게 누른 경우

### 틀린 키
- 뭘 무의식적으로 많이 치는지; 틀리는지

### 세션
- 엔터에 할당
- cpm, wpm, 연습시간, 기간별 등등등



## 저장을 어떤 형식으로 할지
세션 개념이 필요할거 같기는 함.

### DB 및 저장 방식 관련 검토 사항
* 데이터 구조 (Run > TargetText > KeyEvent)
  * 한 Run(연습 한 세트) 내에 여러 TargetText가 들어가고, 각 문장 안에 개별 KeyEvent(키 입력 이벤트)가 속하는 계층형 JSON 구조 설계 가능.
* DB 옵션 및 아키텍처 비교
  1. Supabase (JSONB 형태 저장 - 1 Run = 1 Row)
     * 장점: 로그인(Auth), DB, 스토리지 원클릭 제공 및 간편한 연동 SDK.
     * 단점: Supabase 무료 티어의 50,000행(Row) 제한 우회를 위해 전체 키 이벤트를 하나의 JSONB 컬럼에 통째로 묶어서 저장해야 함. (SQL 기반 상세 집계 및 통계 쿼리가 다소 복잡해짐)
  2. Neon DB (Row 단위 저장 - 1 KeyEvent = 1 Row)
     * 장점: 무료 티어 행(Row) 제한이 없어 정석적인 관계형 설계 가능. SQL을 사용해 특정 키들의 평균 지연시간 등 고차원 통계 쿼리를 매우 쉽고 빠르게 수행 가능.
     * 단점: 순수 DB만 제공하므로 Clerk(인증/무료 1만 MAU) 같은 로그인 서비스를 별도로 연동해 주어야 함.


TODO: 스키마 짰으니까 프론트에서 저장하는 방식 맞춰. 일단 db는 아직 없으니까 알아서 잘 하셈. 지금은 임시임.

### 구체적인 저장 방법
일단 page 스키마에서 created_at 를 좀 세분화 하자. 시작하는 시점이랑 끝나는 시점 두 개로 나눠.
run 을 끝내는 조건을 생각해보자.
실시간으로 끝나는걸 확인할 수는 없어. 왜냐면 입력하다가 그냥 끄면 끝인데 이걸 감지를 못함. 아마도?
시간 경과로 알아야됨.
page 에 처음글자 입력을 시작할 때:
  가장 최근 run 객체를 확인:
    run이 in progress 이고, page 시점이 최근(5분이내)이면:
      그 run을 fk로 하는 page 생성하고 진행.
    pending 상태라면:
      그 pending run을 삭제하고, 지금 시점으로 pending run을 생성하고 진행.
    run 이 in progress 이고, 마지막 page 의 끝나는 시간이 지금으로부터 5분보다 이전이면:
    (끝나는 시간이 있는 page는 마지막 page가 아닐 수도 있음.)
      그 page를 마지막으로 하고, 이후에 끝나는 시간이 없는 page는 삭제.
      그리고 run 의 status를 completed로 바꾸고 run을 마무리.
      run의 끝나는 시점도 마지막 page의 끝나는 시점으로 함.
    status가 completed 이거나 run이 없으면 pending run을 새로 만들고 시작.
      

넘어갈 수 있는 조건부터 생각을 해야됨.
  스페이스나 엔터로 넘어가려면 끝까지 입력을 해야됨.
  정확도 수치가 있으니까 글자수만 맞추면 스페이스, 엔터로 넘어가지도록.
  그리고 스킵이 있어야함. 그냥 그 글을 입력하기 싫을 수도 있음.
  이거는 오른쪽 방향키로. 방향키로 왔다갔다 하는 로직은 이미 있지 않나.
  좀 더 구체화 해보자.
  이미 지난 page를 왼쪽 방향키로 돌아간다면 같은 글을 다른 page 객체로 다시 새로 시작하는것처럼.
  오른쪽 방향키는 그냥 스킵. 입력을 하는 순간 page가 생기는거니까 아무 작업 안해도 되지 않나.


page가 완료될 때(스킵 아님):
  시작 시점이 오래 전이면(10분보다 이전):
    run을 새로 만들어서 fk 교체.
    이전 run은 completed로 마무리.
    run이랑 page의 시작 시간은 현재에서 긴 공백(latency) 이후의 latency를 다 합친 값을 뺀 값으로 설정.
  가장 최근 run 객체를 확인:
    pending 이라면:
      run을 in progress 로 바꾸고, fk를 그 run으로 지정 후 진행.
    in progree 라면:
      정상적으로 진행.


접속했을 때, 로컬 스토리지를 정리하자. 이전에 안끝난 page는 없애고, run도 마무리.
이러면 page 입력이 글의 중간부터 시작되는 경우는 사라짐.
5분보다 이전인 경우만 run 마무리. 최근이면 page는 지우되, run은 유지.



TODO:
 [예외 처리] 백스페이스 외의 제어/방향키 처리 누락
filterBackspaces 함수는 오직 '알파벳'과 '백스페이스' 간의 관계만 고려하고 있습니다.
문제점: 사용자가 타자 도중 Shift, Enter, 방향키, Ctrl 등을 누르는 시간도 알파벳 간의 지연 시간으로 잘못 묶일 수 있습니다. 예를 들어, 대문자 'A'를 치기 위해 Shift -> A를 눌렀을 때, 직전 소문자와 'A' 사이의 공간적 이동 시간이 아닌 'Shift를 누른 시간'이 개입되어 동역학적 의미가 오염됩니다.

TODO:
shift 는 개별 to key 보여줄 때(꽃) shift 쓰면서 누를 때 얼마나 delay되는지 통계 내주는 식으로.
